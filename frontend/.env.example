VITE_API_URL=http://localhost:8000/api/v1
# Exibe o plano de exemplo quando a API de otimizacao ainda nao responde.
# Defina como false para que a tela mostre apenas o erro real.
VITE_DEMO_FALLBACK=true
